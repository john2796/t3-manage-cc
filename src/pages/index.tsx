import AuthShowcase from "@/components/AuthShowcase";
import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      this is the main page
      <AuthShowcase />
    </div>
  );
};

export default Home;
